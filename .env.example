# ===========================================
# FRONTEND ENVIRONMENT VARIABLES
# ===========================================

# API Configuration (only required variable currently used in code)
NEXT_PUBLIC_API_URL=http://localhost:8001
# For production set to: https://your-backend-api.com

# Optional: local backend port override for dev rewrites (see next.config.js)
# DEV_BACKEND_PORT=8000

# (Removed unused NEXTAUTH and analytics vars – add when implementing those features)